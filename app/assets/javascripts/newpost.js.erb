//= require taglist
//= require tagger


// 'search' for search posts based on tags
// 'post' for posting a new post with tags to post#new
var mode = 'post';
var screenState = 0;

var taglist   = new TagList();
var tagger    = new Tagger();
